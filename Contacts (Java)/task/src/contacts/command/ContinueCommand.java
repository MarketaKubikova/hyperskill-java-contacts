package contacts.command;

/**
 * Command to continue handling the current menu
 */
public class ContinueCommand implements Command {
    @Override
    public void execute() {
        // Continue the loop
    }
}
